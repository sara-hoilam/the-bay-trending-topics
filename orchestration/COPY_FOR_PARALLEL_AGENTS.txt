Use these in **parallel Cursor Agent chats** after Trend Watch is fresh. Attach files as shown in each block.

**Run order (required):**

1. **CHAT T** — refresh Trend Watch (48h boards + `topicCandidates` scores)  
2. **CHAT A / B / C** — in parallel: Top 10 briefings from Trend Watch scores  
3. **CHAT D** — Live Edition merger  
4. `node scripts/merge-briefing-panels.mjs` — merges fragments + stamps masthead date (HKT)

================================================================================
CHAT T — model: any (browsing-capable recommended)
Fragment file: orchestration/fragments/trendwatch.html
================================================================================
You refresh the **Trend Watch** panel in GBA Pulse.

Read and follow **every** instruction in:

  @prompts/gba-pulse-trend-watch-agent-prompt.md
  @prompts/gba-pulse-trend-scoring.md

**Important:** For **Google Trends**, use the **past 48 hours** window only (site often defaults to ~4h — switch to 48h; every `sourceUrl` must include `hours=48`). Capture volumes from the **Trending Now** web table per geo — **not** RSS.

**When this prompt is sent**, pull **real-time** data from live boards (Google 48h per geo, Baidu realtime, TikTok hashtags, X/trends proxy). Update **only** the JSON inside `<script type="application/json" id="trend-watch-data">…</script>` in `orchestration/fragments/trendwatch.html`. Keep the `model-tag` line, `<svg>` sprite (`tw-icon-*`), and `<div id="trend-watch-root"></div>` unchanged.

**Required in JSON:** `windowHours: 48`, all `sections`, and **`topicCandidates`** array scored per `gba-pulse-trend-scoring.md` (volume + velocity + cross-platform → `compositeScore`).

Do not add the standalone preview ribbon.

After saving, run `node scripts/merge-briefing-panels.mjs` from repo root.

================================================================================
CHAT A — model: Claude Sonnet 4.6
Fragment file: orchestration/fragments/claude.html
================================================================================
**Prerequisite:** CHAT T finished — `orchestration/fragments/trendwatch.html` is fresh (48h boards).

Attach:

  @prompts/gba-pulse-curator-prompt.md
  @prompts/gba-pulse-trend-scoring.md
  @orchestration/fragments/trendwatch.html
  @references/source-links.md
  @templates/gba-pulse-template.html

You are producing ONLY the inner HTML for the **Claude** panel of GBA Pulse.

**Workflow:** Score all Trend Watch candidates → pick **Top 10** by `compositeScore` → research **why each is trending in the last 48h** → write HTML. Do **not** lead with news articles older than 48h unless the topic is live on Trend Watch **now**.

- `.topic-meta` 📊 line: `Trend score: NN/100 · N platforms (…)`
- `.why-box`: lead with `<code>Google Trends HK</code>`, `<code>Baidu Hot Search</code>`, etc. from the capture
- Source/date span after location; `Posted:` from verification article (prefer ≤48h)

Same CSS classes as template. No `<html>`, `<head>`, outer `<main>`. Write to `orchestration/fragments/claude.html`.

================================================================================
CHAT B — model: Cursor Composer 2
Fragment file: orchestration/fragments/composer.html
================================================================================
**Prerequisite:** CHAT T finished — `orchestration/fragments/trendwatch.html` is fresh (48h boards).

Attach:

  @prompts/gba-pulse-curator-prompt.md
  @prompts/gba-pulse-trend-scoring.md
  @orchestration/fragments/trendwatch.html
  @references/source-links.md
  @templates/gba-pulse-template.html

Same workflow as CHAT A for the **Composer** panel. Write to `orchestration/fragments/composer.html`.

================================================================================
CHAT C — model: GPT-5.5
Fragment file: orchestration/fragments/chatgpt.html
================================================================================
**Prerequisite:** CHAT T finished — `orchestration/fragments/trendwatch.html` is fresh (48h boards).

Attach:

  @prompts/gba-pulse-curator-prompt.md
  @prompts/gba-pulse-trend-scoring.md
  @orchestration/fragments/trendwatch.html
  @references/source-links.md
  @templates/gba-pulse-template.html

Same workflow as CHAT A for the **GPT-5.5** panel. Write to `orchestration/fragments/chatgpt.html`.

================================================================================
AFTER A, B, C FINISH
================================================================================
Run CHAT D, then:

  node scripts/merge-briefing-panels.mjs

================================================================================
CHAT D — model: GPT-5.5 or Claude Sonnet 4.6
Fragment file: orchestration/fragments/overall.html
================================================================================
You are the **Live Edition** merger for GBA Pulse.

Read:

  @orchestration/fragments/trendwatch.html
  @orchestration/fragments/claude.html
  @orchestration/fragments/composer.html
  @orchestration/fragments/chatgpt.html
  @prompts/gba-pulse-curator-prompt.md
  @prompts/gba-pulse-trend-scoring.md

Create merged **Top 10** for Live Edition.

**Merge rules:**

1. **Anchor on Trend Watch:** prefer topics with high `compositeScore` in `topicCandidates` and strong cross-platform hits; deprioritize model-only stories not on Trend Watch.
2. Normalize duplicates across the three model outputs.
3. Rank by: Trend Watch composite score → cross-model overlap → GBA relevance → freshness (48h) → verification quality.
4. Do **not** promote stale news (articles weeks old) unless Trend Watch shows a **current** spike; explain in summary.
5. Keep reference mix where possible: innovation/business ~50%, arts/culture ~30%, food/drink ~15%, other ~15%; mainland GBA ~50%, HK ~30%, Macao ~20%.
6. Every `.topic-meta`: source/date after location; 📊 includes `Trend score: NN/100 · N platforms`.
7. Every `.why-box`: `<strong>Why It's Trending</strong>` + 1–3 short `<li>` with `<code>` platform chips from Trend Watch capture.

Output ONLY inner HTML: `<p class="agg-method">…</p>`, ten `.topic` blocks, Key Observations, Sources, Notes for Tomorrow. No `<html>`, `<head>`, `<body>`, outer `<main>`.

Write to `orchestration/fragments/overall.html`.

After saving, run `node scripts/merge-briefing-panels.mjs` (refreshes masthead/footer dates in HKT). Use `--no-stamp` only if you must skip date refresh. Commit and deploy.
